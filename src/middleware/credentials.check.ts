import { NextFunction, Request, Response } from "express";


// interface  checkType {
// 	field : keyof Request
// 	from: string
	
// }

// export const CheckCredentialsExists = ({ field } : checkType) =>  (req: Request, res : Response, next: NextFunction) => {

// }